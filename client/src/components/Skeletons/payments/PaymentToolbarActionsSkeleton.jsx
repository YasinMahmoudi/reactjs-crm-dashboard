import { IconButton } from '@mui/material';
import MoreVertIcon from '@mui/icons-material/MoreVert';

export default function PaymentToolbarActionsSkeleton() {
  return (
    <IconButton>
      <MoreVertIcon />
    </IconButton>
  );
}
